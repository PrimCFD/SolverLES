#include <iostream>

/// \brief Entry point for SolverLES.
/// Initializes the solver and runs the selected case.

int main()
{
    std::cout << "Solver bootstrap running." << std::endl;
    return 0;
}