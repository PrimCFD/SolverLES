name: Style Check

on:
  pull_request:

jobs:
  style:
    name: Check Code Formatting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install formatting tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format python3-pip
          pip install --upgrade pip
          pip install fprettify cmake-format

      - name: Run format-all.sh
        run: bash scripts/format-all.sh

      - name: Check for unformatted files
        run: |
          if [[ $(git status --porcelain) ]]; then
            echo "❌ Formatting issues detected! Please run:"
            echo "   bash scripts/format-all.sh"
            echo
            echo "🔍 Diff preview:"
            git --no-pager diff
            exit 1
          else
            echo "✅ All files are properly formatted."
          fi
